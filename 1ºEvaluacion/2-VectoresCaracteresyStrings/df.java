
package vectorescaracteresystrings;
public class df {
    public static void main(String[] args) {
        
    }
}
//frase.charAt(5)
//nombres[0].compareTo(nombres[1]);
//frase.substring(3, 11)
//frase=frase.replace('a', 'Y');/frase=frase.replaceAll("En ", "XXXX");//frase[i]=frase[i].replace(subCadena, subCadena.toUpperCase());
//int posicion= frase[i].indexOf(pal);
//frase[0]=frase[0].concat(" hola");

/* int posicion=frase.indexOf(" ");
        while(posicion != -1){
            frase=frase.substring(0, posicion).concat(frase.substring(posicion+1,frase.length()));
            posicion= frase.indexOf(" "); */

/*if(frase[i]==sustituir){
                frase[i]=sustituto[0];
                for(int z=1;z<sustituto.length;z++){
                    for(int p=frase.length-1;p>i+1;p--)
                        frase[p]=frase[p-1];
                    i++;
                    frase[i]=sustituto[z];*/

//Character.toString('a')